# Production Environment Variables
# Copy this to your hosting provider's environment variable configuration

# Server Configuration
PORT=3000
HOST=0.0.0.0
NODE_ENV=production

# Database Configuration
# Most cloud providers give you a DATABASE_URL
DATABASE_URL=postgresql://user:password@host:5432/dbname

# Or use individual components
# DATABASE_HOST=your-db-host.render.com
# DATABASE_PORT=5432
# DATABASE_NAME=budget
# DATABASE_USER=budget_user
# DATABASE_PASSWORD_ENCRYPTED=<output from npm run encrypt-secret>

# JWT Configuration
# Generate a strong random secret: openssl rand -base64 32
JWT_SECRET=<generate-strong-random-secret>
JWT_EXPIRES_IN=24h

# Plaid Configuration
# Get credentials from: https://dashboard.plaid.com/
PLAID_CLIENT_ID_ENCRYPTED=<plaid client ID encrypted with encryption key>
PLAID_SECRET_ENCRYPTED=<plaid secret encrypted with encryption key>
PLAID_ENV=production
PLAID_PRODUCTS=transactions
PLAID_COUNTRY_CODES=US

# reCAPTCHA Configuration
# Get credentials from: https://www.google.com/recaptcha/admin
RECAPTCHA_SECRET_KEY=<encrypted recaptcha secret key>

# Encryption Key
# Generate with: openssl rand -base64 32
# CRITICAL: Store this securely and never commit to git
ENCRYPTION_KEY=<generate-strong-encryption-key>

# CORS Configuration (optional)
# Comma-separated list of allowed origins
# CORS_ORIGIN=https://yourdomain.com,https://www.yourdomain.com
