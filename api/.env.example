# Server Configuration
PORT=3000
HOST=0.0.0.0

# Database Configuration
# Option 1: Full connection string (takes precedence)
# DATABASE_URL=postgresql://user:password@localhost:5432/budget

# Option 2: Individual components
DATABASE_HOST=localhost
DATABASE_PORT=5432
DATABASE_NAME=budget
DATABASE_USER=postgres

# For local development: use plain password
DATABASE_PASSWORD=your-local-password

# For production: use encrypted password
# DATABASE_PASSWORD_ENCRYPTED=<output from npm run encrypt-secret>
# ENCRYPTION_KEY=your-encryption-key

# JWT Configuration
JWT_SECRET=change-this-to-a-secure-random-string
JWT_EXPIRES_IN=24h
